import React from 'react'
import type { ReactElement } from 'react'
import { Title } from '@mantine/core'

export const Logo = (): ReactElement => {
  return (
    <Title  color={'dimmed'} fw='bolder' ff={'monospace'} order={3}>KAIPULLA</Title>
  )
}
